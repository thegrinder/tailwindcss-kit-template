---
name: Spacing
menu: Utilities
route: utilities/spacing
---

# Spacing

import { Playground } from 'docz';
import resolveConfig from 'tailwindcss/resolveConfig';
import { ClassNamesTable } from '../../docz-components';
import { convertToDocsTable } from '../../helpers';
import { getMarginClassNames, getPaddingClassNames } from './spacing';

## Padding

<ClassNamesTable
  data={convertToDocsTable(getPaddingClassNames(resolveConfig().theme.spacing))}
/>

## Margin

<ClassNamesTable
  data={convertToDocsTable(getMarginClassNames(resolveConfig().theme.spacing))}
/>
