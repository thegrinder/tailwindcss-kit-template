---
name: Border radius
menu: Utilities
route: /utilities/border-radius
---

# Border radius

import resolveConfig from 'tailwindcss/resolveConfig';
import { ClassNamesTable } from '../../docz-components';

<ClassNamesTable
  data={Object.keys(resolveConfig().theme.borderRadius).reduce(
    (acc, size) => ({
      ...acc,
      [`br-${size}`]: [
        `border-radius: ${resolveConfig().theme.borderRadius[size]}`,
      ],
    }),
    {}
  )}
/>
