---
name: Font size
menu: Utilities
route: /utilities/font-size
---

# Font size

import { ClassNamesTable, ResponsivenessTable } from '../../docz-components';
import { convertToDocsTable } from '../../helpers';
import { fontSizeTheme } from './font-size-theme';

## Variants

<ResponsivenessTable />

## Classes

<ClassNamesTable
  data={convertToDocsTable(
    Object.keys(fontSizeTheme).reduce(
      (acc, key) => ({
        ...acc,
        [`text-${key}`]: {
          fontSize: fontSizeTheme[key],
        },
      }),
      {}
    )
  )}
/>
