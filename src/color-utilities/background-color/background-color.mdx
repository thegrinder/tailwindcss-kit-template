---
name: Background color
menu: Color utilities
route: color-utilities/background-color
---

# Background color

import { Playground } from 'docz';
import { useColorMode } from 'theme-ui';
import { colors } from '../../colors';
import { ClassNamesTable } from '../../docz-components';
import { mapNestedThemeToClasses } from '../../helpers';

<ClassNamesTable
  data={mapNestedThemeToClasses(colors, (colorMode, variant, num) => ({
    [`${colorMode}:bg-${variant}-${num}`]: [
      `background-color: ${colors[colorMode][variant][num]}`,
    ],
  }))}
/>

<Playground>
  {() => {
    const [colorMode] = useColorMode();
    return Object.keys(colors[colorMode]).map((variant) => (
      <div key={variant} className="flex mb-4">
        {Object.keys(colors[colorMode][variant]).map((num) => (
          <div
            key={`${colorMode}-${variant}-${num}`}
            className={`${colorMode}:bg-${variant}-${num} h-16 w-16`}
          />
        ))}
      </div>
    ));
  }}
</Playground>
