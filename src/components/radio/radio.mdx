---
name: Radio
menu: Components
route: /components/radio
---

# Radio

import { Playground } from 'docz';
import Radio from './ReactRadio';
import OutlinedRadio from './ReactOutlinedRadio';

## Simple radio

### React snippet

```jsx
import React from 'react';
import classNames from 'classnames';
import { useColorMode } from 'theme-ui';

const Radio = ({
  valid = false,
  invalid = false,
  disabled = false,
  className,
  checked,
  id,
}) => {
  const [colorMode] = useColorMode();
  return (
    <input
      id={id}
      type="radio"
      checked={checked}
      disabled={disabled}
      className={classNames({
        radio: true,
        [className]: !!className,
        [`${colorMode}:radio-normal`]: !valid && !invalid,
        [`${colorMode}:radio-valid`]: valid,
        [`${colorMode}:radio-invalid`]: invalid,
      })}
    />
  );
};
```

<Playground>
  <label htmlFor="simple-radio-unchecked" className="flex mb-4">
    <Radio id="simple-radio-unchecked" className="mr-2" />
    Unchecked
  </label>
  <label htmlFor="simple-radio-checked" className="flex mb-4">
    <Radio id="simple-radio-checked" checked className="mr-2" />
    Checked
  </label>
  <label htmlFor="simple-radio-disabled" className="flex mb-4">
    <Radio id="simple-radio-disabled" disabled className="mr-2" />
    Disabled
  </label>
  <label htmlFor="simple-radio-disabled-checked" className="flex mb-4">
    <Radio
      id="simple-radio-disabled-checked"
      disabled
      checked
      className="mr-2"
    />
    Disabled and checked
  </label>
  <label htmlFor="simple-radio-invalid" className="flex mb-4">
    <Radio id="simple-radio-invalid" invalid className="mr-2" />
    Invalid
  </label>
  <label htmlFor="simple-radio-valid" className="flex mb-4">
    <Radio id="simple-radio-valid" valid className="mr-2" />
    Valid
  </label>
  <label htmlFor="simple-radio-valid-checked" className="flex mb-4">
    <Radio id="simple-radio-valid-checked" valid checked className="mr-2" />
    Valid and checked
  </label>
</Playground>

## Outlined radio

### React snippet

```jsx
import React from 'react';
import OutlinedLabel from '../outlined-label/ReactOutlinedLabel';
import Radio from './ReactRadio';

const OutlinedRadio = ({
  invalid = false,
  valid = false,
  disabled = false,
  checked,
  name,
  label,
  className,
}) => (
  <OutlinedLabel
    htmlFor={name}
    invalid={invalid}
    valid={valid}
    label={label}
    className={className}
    disabled={disabled}
  >
    <Radio
      checked={checked}
      valid={valid}
      invalid={invalid}
      disabled={disabled}
      id={name}
    />
  </OutlinedLabel>
);
```

<Playground>
  <OutlinedRadio
    label="Unchecked"
    name="outlined-radio-unchecked"
    className="mb-4"
  />
  <OutlinedRadio
    label="Checked"
    name="outlined-radio-checked"
    checked
    className="mb-4"
  />
  <OutlinedRadio
    label="Disabled"
    name="outlined-radio-disabled"
    disabled
    className="mb-4"
  />
  <OutlinedRadio
    label="Disabled and checked"
    name="outlined-radio-disabled-checked"
    disabled
    checked
    className="mb-4"
  />
  <OutlinedRadio
    label="Invalid"
    name="outlined-radio-invalid"
    invalid
    className="mb-4"
  />
  <OutlinedRadio
    label="Valid"
    name="outlined-radio-valid"
    valid
    className="mb-4"
  />
  <OutlinedRadio
    label="Valid and checked"
    name="outlined-radio-valid-checked"
    valid
    checked
    className="mb-4"
  />
</Playground>
