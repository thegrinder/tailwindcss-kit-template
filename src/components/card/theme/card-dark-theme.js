const { darkTheme } = require('../../../colors');

const cardDarkTheme = {
  1: {
    backgroundColor: darkTheme.neutral[50],
  },
  2: {
    backgroundColor: darkTheme.neutral[100],
  },
};

module.exports = cardDarkTheme;
