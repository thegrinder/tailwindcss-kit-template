---
name: Text
menu: Components
route: components/text
---

# Text

import { Playground } from 'docz';
import { useColorMode } from 'theme-ui';

## Colors

<Playground>
  {() => {
    const [colorMode] = useColorMode();
    return (
      <>
        <div className="mb-12">
          <p className={`${colorMode}:text-color-neutral--high text-base`}>
            The spectacle before us was sublime.
          </p>
          <p className={`${colorMode}:text-color-primary--high text-base`}>
            The spectacle before us was sublime.
          </p>
          <p className={`${colorMode}:text-color-success--high text-base`}>
            The spectacle before us was sublime.
          </p>
          <p className={`${colorMode}:text-color-warning--high text-base`}>
            The spectacle before us was sublime.
          </p>
          <p className={`${colorMode}:text-color-danger--high text-base`}>
            The spectacle before us was sublime.
          </p>
        </div>
      </>
    );
  }}
</Playground>

## Emphasis

<Playground>
  {() => {
    const [colorMode] = useColorMode();
    return (
      <>
        <div className="mb-12">
          <p className={`${colorMode}:text-color-neutral--high text-base`}>
            The spectacle before us was sublime.
          </p>
          <p className={`${colorMode}:text-color-neutral--medium text-base`}>
            The spectacle before us was sublime.
          </p>
          <p className={`${colorMode}:text-color-neutral--low text-base`}>
            The spectacle before us was sublime.
          </p>
        </div>
      </>
    );
  }}
</Playground>
