---
name: Heading
menu: Components
route: components/heading
---

# Heading

import { Playground } from 'docz';
import { useColorMode } from 'theme-ui';

## Emphasis

<Playground>
  {() => {
    const [colorMode] = useColorMode();
    return (
      <>
        <div className="mb-12">
          <h5 className={`${colorMode}:heading-color-neutral--high text-xl`}>
            The spectacle before us was sublime.
          </h5>
          <h5 className={`${colorMode}:heading-color-neutral--medium text-xl`}>
            The spectacle before us was sublime.
          </h5>
          <h5 className={`${colorMode}:heading-color-neutral--low text-xl`}>
            The spectacle before us was sublime.
          </h5>
        </div>
      </>
    );
  }}
</Playground>
