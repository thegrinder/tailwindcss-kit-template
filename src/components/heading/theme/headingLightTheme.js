const { lightTheme } = require('../../../colors');

const headingLightTheme = {
  neutral: {
    high: lightTheme.neutral1000,
    medium: lightTheme.neutral800,
    low: lightTheme.neutral700,
  },
};

module.exports = headingLightTheme;
