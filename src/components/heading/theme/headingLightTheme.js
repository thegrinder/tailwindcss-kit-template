const { lightTheme } = require('../../../colors');

const headingLightTheme = {
  neutral: {
    high: lightTheme.neutral[1000],
    medium: lightTheme.neutral[800],
    low: lightTheme.neutral[700],
  },
};

module.exports = headingLightTheme;
