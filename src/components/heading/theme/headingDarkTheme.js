const { darkTheme } = require('../../../colors');

const headingDarkTheme = {
  neutral: {
    high: darkTheme.neutral[900],
    medium: darkTheme.neutral[800],
    low: darkTheme.neutral[700],
  },
};

module.exports = headingDarkTheme;
