---
name: Link
menu: Components
route: components/link
---

# Link

import { Playground } from 'docz';
import { useColorMode } from 'theme-ui';

## Types

<Playground>
  {() => {
    const [colorMode] = useColorMode();
    return (
      <>
        <div className="mb-4">
          <a className={`link ${colorMode}:link-variant-default`} href="#">
            default
          </a>
        </div>
        <div className="mb-4">
          <a className={`link ${colorMode}:link-variant-muted`} href="#">
            muted
          </a>
        </div>
      </>
    );
  }}
</Playground>
